import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function InitiatePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Initiate Company Process</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is where the form to initiate a new company process would go.</p>
      </CardContent>
    </Card>
  );
}
